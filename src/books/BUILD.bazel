load("@rules_rust//rust:defs.bzl", "rust_binary")

package(default_visibility = ["//visibility:public"])

exports_files(["data.toml"])

rust_binary(
    name = "books",
    srcs = ["main.rs"],
    data = [":data.toml"],
    deps = [
        "@crates//:clap",
        "@crates//:serde",
        "@crates//:toml",
    ],
)
