create a tags system for the site

implement as a sphinx extension at
src/_extensions/tags.py

tags should be specified as field list at top of
a blog post e.g.

:tags: vim

extension should validate that only allowlisted tags
are being used
